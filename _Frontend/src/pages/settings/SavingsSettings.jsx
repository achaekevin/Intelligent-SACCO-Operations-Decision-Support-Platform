import StubPage from '../../components/common/StubPage'

const SavingsSettings = () => (
  <StubPage
    title="Savings Settings"
    subtitle="Configure savings products"
    sections={["Minimum balances", "Withdrawal rules"]}
  />
)

export default SavingsSettings
