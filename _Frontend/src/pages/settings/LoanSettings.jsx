import StubPage from '../../components/common/StubPage'

const LoanSettings = () => (
  <StubPage
    title="Loan Settings"
    subtitle="Configure loan products and rules"
    sections={["Loan types", "Interest rates", "Limits"]}
  />
)

export default LoanSettings
