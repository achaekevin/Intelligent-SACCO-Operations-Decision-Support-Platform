import StubPage from '../../components/common/StubPage'

const TransactionList = () => (
  <StubPage
    title="Transactions"
    subtitle="All deposits, withdrawals, transfers and reversals"
    sections={["Search", "Filters", "Printable receipts"]}
  />
)

export default TransactionList
