import StubPage from '../../components/common/StubPage'

const LoginHistory = () => (
  <StubPage
    title="Login History"
    subtitle="Login attempts and sessions"
    sections={["IP address", "Status"]}
  />
)

export default LoginHistory
