# Base URL for the real backend API. The app runs fully on mock data
# (see src/utils/mockData.js) until you point this at a live backend
# and wire up src/services/api.js calls inside the Redux slices.
VITE_API_BASE_URL=http://localhost:8000/api
